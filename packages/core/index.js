'use strict';

// Placeholder release (0.0.1) reserving the `urlic` name on npm.
// The real synchronous, offline `inspect()` implementation is in progress —
// see the PRD and docs/architecture.md in the repository.

function inspect() {
  throw new Error(
    'urlic@0.0.1 is a name-reservation placeholder; inspect() is not implemented yet.'
  );
}

module.exports = { inspect };
