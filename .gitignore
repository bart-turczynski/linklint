.env
.env.*
!.env.example
*.log
.DS_Store

# Fiberplane local issue tracking (backed up separately by tools/backup.sh —
# `git bundle` cannot contain it).
.fp/
_scratch/

# Heavy artifacts — never commit (a big blob bloats .git history even after deletion).
*.db
*.db-shm
*.db-wal
*.sqlite
*.sqlite3
*.faiss
*.npy
*.npz
vectors/

node_modules/
dist/
coverage/
